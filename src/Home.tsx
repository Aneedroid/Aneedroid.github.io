import { Box } from '@mui/material';
import Hero from './Hero';

const Home = () => {
  return (
    <Box>
      <Hero />
      {/* You can add <About />, <Experience />, <Projects /> here later */}
    </Box>
  );
};

export default Home;
